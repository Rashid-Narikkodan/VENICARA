<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Server Error | VENICARA</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      background: linear-gradient(135deg, #fdfbfb, #ebedee);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .error-container {
      background: #fff;
      padding: 3rem 2rem;
      border-radius: 15px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
      max-width: 600px;
      width: 90%;
      text-align: center;
    }

    .error-container h1 {
      font-size: 6rem;
      color: #dc3545;
      margin-bottom: 1rem;
      text-shadow: 2px 2px 10px rgba(0,0,0,0.1);
    }

    .error-container p {
      margin-bottom: 1rem;
      font-size: 1.2rem;
    }

    .btn-home, .btn-show-error {
      border: none;
      padding: 0.75rem 2rem;
      font-size: 1.1rem;
      border-radius: 50px;
      transition: all 0.3s ease;
      margin: 0.5rem;
      cursor: pointer;
    }

    .btn-home {
      background: black;
      color: white;
    }

    .btn-show-error {
      background: linear-gradient(90deg, #dc3545, #ff6b6b);
      color: white;
    }

    .btn-home:hover, .btn-show-error:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
    }

    .error-message {
      margin-top: 1.5rem;
      padding: 1rem;
      background-color: #f8d7da;
      color: #721c24;
      border-radius: 10px;
      display: none; /* hidden by default */
    }

    .illustration {
      margin-top: 2rem;
      max-width: 300px;
      opacity: 0.85;
    }

    @media(max-width: 768px){
      .error-container h1 {
        font-size: 4.5rem;
      }
      .illustration {
        max-width: 200px;
      }
    }
  </style>
</head>
<body>
  <div class="error-container">
    <h1>500</h1>
    <p class="fs-4">Oops! Something went wrong on our server.</p>
    <p class="text-muted mb-4">Please try again later or go back to the previous page.</p>

    <!-- Buttons -->
    <button class="btn btn-home btn-lg shadow" onclick="history.back()">Go Back</button>
    <button id="showErrorBtn" class="btn btn-show-error btn-lg shadow">Show Error</button>

    <!-- Div to display error -->
    <div id="errorDiv" class="error-message">
     <%=message|| "500 Internal Server Error: Something went wrong!"%>
    </div>

  </div>

  <!-- JS -->
  <script>
    const showErrorBtn = document.getElementById('showErrorBtn');
    const errorDiv = document.getElementById('errorDiv');

    showErrorBtn.addEventListener('click', () => {
      errorDiv.style.display = errorDiv.style.display === 'none' ? 'block' : 'none';
    });
  </script>
</body>
</html>
