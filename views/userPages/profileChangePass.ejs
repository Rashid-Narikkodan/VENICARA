<!DOCTYPE html>
<html lang="en">
 <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%=title%></title>
  <link
   href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css"
   rel="stylesheet"
  />
  <script defer src="/js/forgotPassChange.js"></script>
  <link rel="stylesheet" href="/css/userAuth.css" />
 </head>
 <body class="vh-100 d-flex align-items-center">
  <div class="container text-center d-flex justify-content-center">
   <div
    class="card shadow-lg border-0 rounded-5 overflow-hidden mt-3"
    style="max-width: 800px; width: 100%"
   >
    <div class="row g-0 d-flex justify-content-center pt-md-5 pb-md-5">
     <!-- Right Form Section -->
     <div class="col-md-5 p-5">
      <h3 class="fw-bolder mb-4 fs-2">New password</h3>
      <p class="mb-5 fw-semibold">Please confirm your password</p>
      <%if(messages.error.length>0){%>
      <div class="alert alert-danger p-2"><%=messages.error[0]%></div>
      <%}%>
      <!-- Form -->
      <form action="/profile/changePassword?_method=PATCH" method="POST" id="form">
       <div class="mb-2">
        <input
         type="password"
         id="oldPass"
         name="oldPassword"
         class="form-control rounded-3 p-3"
         placeholder=" Old password"
        />
        <div id="oldPassError" class="text-danger"></div>
        <input
         type="password"
         id="pass"
         name="password"
         class="form-control rounded-3 p-3 mt-3"
         placeholder=" New password"
        />
        <div id="passError" class="text-danger"></div>
        <input
         type="password"
         id="confirmPass"
         name="confirmPassword"
         class="form-control rounded-3 p-3 mt-2"
         placeholder=" Confirm password"
        />
        <div class="text-danger" id="confirmError"></div>
       </div>
       <button type="submit" class="btn btn-dark w-100 rounded-3 p-2 fs-4 mt-2">
        Change Password
       </button>
      </form>

      <p class="mt-3 text-center text-muted">
       <a href="/profile" class="fw-semibold text-decoration-none text-secondary">Go back to Profile page?</a>
      </p>
     </div>
     <!-- Left Image Section -->
     <div
      class="col-md-6 d-none d-md-flex align-items-center justify-content-center">
      <img
       src="/images/blackshirtwtchperfume.avif"
       alt="Signup"
       class="object-fit-cover img-fluid rounded-4 img-login"
      />
     </div>
    </div>
   </div>
  </div>
 </body>
</html>