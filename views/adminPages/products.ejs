<%- include('../partials/admin/header.ejs')%>
  <%- include('../partials/admin/sidebar.ejs')%>
    <main class="flex-grow-1 min-vh-100 p-3">
      <div class="container-fluid">
        <div class="row align-items-center mb-5">
          <div class="col-md-6">
            <h1>Products List</h1>
          </div>
          <div class="col-md-6 d-flex justify-content-end gap-2">
            <input type="text" class="form-control w-50" placeholder="Search...">
            <button class="btn btn-dark">Search</button>
            <a href="/admin/products/add"><button class="btn btn-dark">New</button></a>
          </div>
        </div>
        <div class="table-responsive shadow rounded-3 bg-white">
          <table class="table table-hover align-middle mb-0">
            <thead class="table-dark">
              <tr>
                <th>No.</th>
                <th>Product</th>
                <th>Product Image</th>
                <th>Base Price</th>
                <th>Category</th>
                <th>Total Stock</th>
                <th>Variants</th>
                <th>Status</th>
                <th>Added On</th>
                <th class="text-center">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% products.forEach(product=> { %>
                <%count++%>
                <tr>
                  <td>
                    <%= count %>
                  </td>
                  <td>
                    <%= product.name %>
                  </td>
                  <td>
                    <img src="" alt="product img" width="50">
                  </td>
                 <td>
                  <%= product.basePrice%>₹
                  </td>
                  <td>
                    <%= product.categoryName %>
                  </td>
                  <td>
                    <%= product.totalStock %>
                  </td>
                  <td>
                    <span class="badge bg-primary">
                    </span>
                  </td>
                  <td>
                    <span class="badge <%= product.isAvailable ? 'bg-success' : 'bg-danger' %>">
                      <%= product.isAvailable ? 'Available' : 'Not availabe' %>
                    </span>
                  </td>
                  <td>
                    <%=product.addedOn%>
                  </td>
                  <td class="text-center">
          
                    <a href="/admin/products/edit/<%=product._id%>"><button class="btn btn-sm btn-outline-primary"><i class="fa fa-pen"></i></button></a>
              
                    <form action="/admin/products/<%=product._id%>?_method=PATCH" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure you want to delete this product?')"><i class="fa fa-trash"></i></button>
                    </form>
                  </td>
                </tr>
                <% }) %>
            </tbody>
          </table>
        </div>
      </div>
      <!-- //pagination -->
      <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center flex-wrap">
          <% if (currentPage> 1) { %>
            <li class="page-item">
              <a class="page-link" href="?page=<%= currentPage - 1 %>&limit=<%= limit %>">
                <i class="fa-solid fa-caret-left"></i>
              </a>
            </li>
            <% } else { %>
              <li class="page-item disabled">
                <span class="page-link"><i class="fa-solid fa-caret-left"></i></span>
              </li>
              <% } %>
                <!-- Page Numbers -->
                <% for(let i=1; i <=totalPages; i++) { %>
                  <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                    <% if (i===currentPage) { %>
                      <span class="page-link">
                        <%= i %>
                      </span>
                      <% } else { %>
                        <a class="page-link" href="?page=<%= i %>&limit=<%= limit %>">
                          <%= i %>
                        </a>
                        <% } %>
                  </li>
                  <% } %>
                    <!-- Next Button -->
                    <% if (currentPage < totalPages) { %>
                      <li class="page-item">
                        <a class="page-link" href="?page=<%= currentPage + 1 %>&limit=<%= limit %>">
                          <i class="fa-solid fa-caret-right"></i>
                        </a>
                      </li>
                      <% } else { %>
                        <li class="page-item disabled">
                          <span class="page-link"><i class="fa-solid fa-caret-right"></i></span>
                        </li>
                        <% } %>
        </ul>
      </nav>
    </main>
    </div>
    <%- include('../partials/admin/footer.ejs')%>